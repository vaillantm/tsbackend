export const removeFile = (filePath: string) => {
  // Placeholder for file removal logic
  console.log(`Placeholder: Removing file: ${filePath}`);
};

export const removeFiles = (filePaths: string[]) => {
  // Placeholder for multiple file removal logic
  console.log(`Placeholder: Removing files: ${filePaths.join(', ')}`);
};
